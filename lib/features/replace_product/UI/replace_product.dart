import 'package:flutter/material.dart';

class ReplaceProduct extends StatelessWidget {
  const ReplaceProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}