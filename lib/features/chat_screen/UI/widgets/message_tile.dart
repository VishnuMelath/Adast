import 'package:flutter/material.dart';

class RecieverTile extends StatelessWidget {
  final bool reciever;
  const RecieverTile({super.key,required this.reciever});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}